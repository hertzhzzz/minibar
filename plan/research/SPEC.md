# MiniBar 规格说明书 (System Specification)

## Problem Statement

随着常驻后台小工具增多，MacBook 用户（尤其是配备物理刘海屏的 M 系列 MacBook Air / Pro）面临严重的菜单栏拥挤问题：
1. **刘海物理遮挡**：macOS 菜单栏左侧为应用菜单，右侧为系统与第三方状态项。当图标数量超过右侧安全物理宽度时，多余的图标会被刘海直接吞没隐藏，用户无法感知也无法访问。
2. **缺乏原生轻量的整合工具**：现有工具要么占用大量常驻内存，要么在 macOS 15 Sequoia 引入频繁的屏幕录制警报，或在折叠/展开时产生剧烈的屏幕闪烁。

## Solution

MiniBar 是一款轻量、原生、纯 Swift 构建的 macOS 菜单栏整合与快捷访问工具：
- 在状态栏保留一个常驻控制图标（SF Symbol）与一个平时展开至 10,000 pt 的推挤分隔符，将需要收纳的图标物理移出主屏幕可视区。
- 点击控制图标展开一个紧凑的 4 列毛玻璃 Popover 网格，显示被收纳应用的高清 Retina 2x 图标。
- 用户点击网格项时，优先通过 Accessibility 协议触发原应用菜单；对非标项通过一帧全屏快照遮罩保底合成物理点击，全过程无感无闪烁。
- 提供内置“整理模式”，用户点击即可在“顶栏常驻”与“抽屉收纳”之间一键切换；支持开机自启。

## User Stories

1. 作为 MacBook 刘海屏用户，我希望启动应用时默认收纳不常用的菜单栏图标，以便顶栏保持清爽且关键图标不被刘海遮挡。
2. 作为用户，我希望点击主控制图标能呼出一个紧凑的 Popover 浮层，以便随时查看被收纳的所有图标。
3. 作为用户，我希望浮层中的图标保持 Retina 高清画质并紧凑排列，以便一眼辨识出对应的应用。
4. 作为用户，当我鼠标悬停在浮层图标上时，我希望看到应用名称 Tooltip，以便区分外观相似的后台图标。
5. 作为用户，当我点击浮层中的微信/网盘等图标时，我希望目标应用的原生菜单能立刻弹出，且我的鼠标光标不会乱跳。
6. 作为用户，当我点击浮层中的某个图标并弹出其菜单后，我希望 MiniBar 的浮层自动关闭，以便不遮挡刚刚弹出的应用菜单。
7. 作为用户，当我点击屏幕其他区域时，我希望 Popover 浮层能自动失焦收起。
8. 作为用户，我希望开启“整理模式”后，能清晰通过蓝点/灰点看到每个图标的常驻/收纳状态。
9. 作为用户，在“整理模式”下点击任意图标，我希望它能平滑切换为常驻顶栏或收纳进抽屉，且不会因为误触发生混乱。
10. 作为用户，我希望系统自带的时钟和控制中心图标始终保留在顶栏最右侧，确保基础系统信息时刻可见。
11. 作为用户，我希望电池和 Wi-Fi 图标可以根据我的喜好自由移入收纳抽屉。
12. 作为用户，我希望在偏好设置中勾选开机自启动，以便每次登录 Mac 时自动进入整洁的菜单栏状态。
13. 作为注重续航和隐私的用户，我希望该应用常驻内存 < 30MB，且不产生任何网络流量或不必要的频繁截图告警。

## Implementation Decisions

1. **双状态栏项拓扑架构**：
   - 注册主入口 `ControlItem` 与紧邻左侧的 `DividerItem`。
   - 折叠态下设置 `DividerItem.length = 10,000` 将左侧项完全挤出屏幕。

2. **CoreGraphics 窗口与应用扫描**：
   - 获取 `layer == 25` 状态栏窗口，结合 `NSRunningApplication` 与 PID，排除硬编码不可移动项（Clock, BentoBox）。
   - 保留第三方应用与可移动系统项（Battery, WiFi）。

3. **Retina 截图与透明边距裁切**：
   - 调用 `CGWindowListCreateImage` 截取 64×66 Retina 原生位图。
   - 算法纯函数化：通过 32 位 RGBA 像素矩阵遍历，根据 Alpha > 12 阈值截取最小非透明矩形。
   - 仅在 Popover 展开或布局变化时懒加载捕获，带内存缓存，避免 Sequoia 录屏提示频发。

4. **双轨点击代理服务（ClickProxy）**：
   - 优先调用 `AXUIElementPerformAction(kAXShowMenuAction / kAXPressAction)`。
   - 若失败，激活全屏一帧遮罩 `MenuBarUpdateMask` 阻断视觉闪烁，临时缩短 Divider 至 8 pt，发送带 `0x33` 窗口 ID 的 `CGEvent`，随后恢复推挤并销毁遮罩。

5. **拖拽重排与整理模式（ItemArranger）**：
   - 点击图标时，根据目标状态计算目标 X 坐标，通过 `CGEvent` 合成 `⌘ + Drag` 拖拽过分隔符。
   - 加入 0.3 秒单操作防抖与加载状态反馈。

6. **SwiftUI Popover 交互与常驻**：
   - 4 列自适应网格，带 Tooltip 与整理模式蓝/灰状态徽标。
   - 菜单唤醒即收起、失焦收起、主图标点击切换。
   - 集成 `SMAppService.mainApp` 开机自启动。

## Testing Decisions

- **外部行为原则**：严格针对输入输出接口编写测试，不依赖私有实现。
- **核心测试接缝**：
  1. Alpha 边缘裁切算法纯函数单元测试。
  2. 窗口扫描与可移动/不可移动分类过滤器单元测试。
  3. 拖拽目标坐标计算纯函数单元测试。

## Out of Scope

- 强制隐藏受系统 SIP 锁定的 Clock 和 Control Center 主图标。
- 多显示器动态菜单栏独立同步。
- 菜单栏着色美化。
